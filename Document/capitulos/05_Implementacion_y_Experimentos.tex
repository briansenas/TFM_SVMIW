\chapter{Implementación y Experimentos}
\section{Diseño experimental}
Todo el código desarrollado se encuentra en el GitHub \code{https://github.com/briansenas/TFM_SVMIW}.
El procedimiento experimental consiste en entrenar
un modelo de aprendizaje profundo capaz de estimar la
altura de objetos y se compara sus predicciones frente al método de calibración manual de cámara y re-proyección geométrica.
A continuación se detallan los resultados obtenidos con cada método, así como su comparación frente a un método de referencia
sencillo que predice siempre el promedio de altura de 1.75 metros.

% \begin{figure}[htp]
%   \centering
%   \includegraphics[width=1\textwidth]{imagenes/chapter5/diagram}
%   \caption[Visualización del procedimiento experimental.]{
%   Visualización del procedimiento experimental.
%   Se entrena un modelo de aprendizaje profundo capaz de estimar la
%   altura de objetos y se compara sus predicciones frente al método de calibración manual de cámara y re-proyección geométrica.
% }\label{fig:Mermaid}
% \end{figure}

\section{Detalles técnicos de implementación}\label{app:TechnicalDetails}
Este proyecto ha sido realizado mayoritariamente con el lenguaje de programación Python,
haciendo uso de procedimientos en otros idiomas por medio de interfaces. Para el desarrollo
y ejecución de los modelos fue necesario el uso de la librería de Pytorch junto con las
librerías CUDA para poder ejecutar los modelos en las tarjetas gráficas de NVIDIA. Para
la extracción de fotogramas (imágenes) de los vídeos se utilizó FFMPEG. Para algunos cálculos
números y de manejo de datos Numpy, Pandas y OpenCV~\cite{opencv_library}. Para el control de
versiones del proyecto se utilizó GitHub en varios repositorios, inyectados como submódulos
en el repositorio principal: \url{https://github.com/briansenas/TFM_SVMIW}
\par
La implementación del método de calibración manual es trivial, utilizando el procedimiento
descrito en la Sección ~\ref{sec:ManualCalibration} se obtienen
los parámetros de las cámaras utilizadas en nuestro conjunto de datos de evaluación y, utilizando las ecuaciones
descritas de proyecciones, se obtienen la estimación de la altura de los individuos en escena.
\par
No obstante, para el caso del método automático, existen varios problemas:
\begin{enumerate}
  \item El modelo
pre-entrenado por Zhu \emph{et al.}~\cite{SVMIW} no está disponible en ningún lado para ser utilizado
directamente.
\item El código de entrenamiento tampoco está disponible. Solo están disponibles trozos parciales
  de algunos métodos y funciones.
\item No se disponen de los mismos datos debido a la eliminación de SUN360~\cite{SUN360}, descrita en la Sección~\ref{sec:SUN360}
\end{enumerate}
Con la labor de investigación, se halló la solución para los problemas descritos anteriormente.
Como alternativa a SUN360~\cite{SUN360}, se encontró el conjunto de datos de SPEC~\cite{SPEC}. El
procedimiento de generación de imágenes descritos en el artículo de referencia~\cite{SVMIW},
es el mismo que describe el repositorio de SPEC~\cite{SPEC}. Una vez solucionado el problema
de los datos, utilizando información del artículo y los trozos de código disponible
se obtuvo un procedimiento de entrenamiento con la librería ``maskrcnn-benchmark''~\cite{MaskRCNN}, con
entrenamiento en múltiples tarjetas gráficas y servidores (por necesidades de escalabilidad y agilidad,
permitiendo distribuir la carga y solventar problemas como la necesidad de memoria para entrenar con varias
imágenes a la vez).
\par
No obstante, el código era inestable (problemas de sincronización de entrenamiento con múltiples tarjetas),
de difícil mantenimiento, adaptación y extensibilidad (debido al desconocimiento de las
intenciones de integración e implementación de los autores). Además, las curvas de entrenamiento parecían estancarse en mínimos
locales como se ve en la Figura~\ref{fig:PreviousImplLoss}. Por estos motivos, se decidió realizar
la implementación en la librería sucesora de ``maskrcnn-benchmark''~\cite{MaskRCNN}, en ``detectron2''~\cite{detectron2}.
\par
De esta forma, se obtuvieron diversos beneficios como la estabilidad en entrenamiento distribuido, mejor uso de la
capacidad de cómputo y mejor legibilidad del código. Entre las razones tenemos:
menos operaciones de copia de datos a la tarjeta gráfica, entrenamiento en precisión mixta (disminuye el uso de memoria,
permitiendo así entrenar con más imágenes o modelos más complejos),
ratio de aprendizaje dinámico, diferente por módulo, con calentamiento y recocido,
mejor gestión de datos de visualización del entreno, con almacenado de imágenes de ejemplo,
vectorización del proceso de estimación de la altura y más.
\par
Además de beneficios en implementación, al utilizar ``detectron2''~\cite{detectron2}, se dispone
de modelos y arquitecturas nuevas con mejores rendimientos en detección. Siguiendo las
directrices de implementación, se desarrolló un modelo más complejo con intención de
obtener resultados similares a los de Zhu \emph{et al.}~\cite{SVMIW}.

\begin{figure}[!ht]
  \centering
  \small
  \includegraphics[width=0.6\textwidth]{imagenes/appendix/loss_plot.pdf}
  \caption[Funciones de pérdidas del modelo de estimación de parámetros de cámara.]{En la Figura se observan las funciones de pérdidas (menor es mejor) del modelo al intentar resolver solamente el problema de estimación de parámetros de la cámara.
    La línea superior verde, casi horizontal, es la implementación obtenida de unir los trozos de código de~\cite{SVMIW}.
    El estancamiento puede deberse a que los autores realizaran modificaciones en la arquitectura que no se reflejan en los trozos de código disponibles ni en el artículo de referencia.
    Por otro lado, la línea inferior naranja representa el error de entrenamiento de la nueva implementación en
    ``detectron2''~\cite{detectron2}. Seguida de su error de validación, línea intermedia azul, que también presenta una tendencia negativa.
    Es evidente la diferencia entre las implementaciones, donde la nueva parece aprender correctamente.
  }
  \label{fig:PreviousImplLoss}
\end{figure}

Para la gestión de los diferentes pasos a realizar, como la calibración manual de las cámaras o el filtrado de datos válidos,
se diseñó un sistema basado en comandos para la ejecución de cada tarea de forma individual, pero permitiendo conexiones sencillas
entre los pasos de manera secuencial como ilustra la Figura~\ref{fig:ScriptsDesign}.

\begin{figure}[!ht]
  \begin{center}
    \includegraphics[width=0.9\textwidth]{imagenes/ScriptsDesign.png}
  \end{center}
  \caption[Abstracción del sistema de comandos implementado.]{Se observa la abstracción del sistema por comandos creados para la realización de parte de este proyecto.
  Destacan los métodos de calibración, rectificación y estimación de la altura.
}\label{fig:ScriptsDesign}
\end{figure}
\subsection*{Preprocesado de datos}
Se utiliza preprocesados existentes en la librería ``detectron2''~\cite{detectron2}, que incorpora
tanto estabilidad durante el entrenamiento como eficiencia computacional. Incluye transformación
de las imágenes a tensores, normalización mediante medias y desviaciones estándar por canal de color
extraídos de ImageNet~\cite{ImageNet} (debido a que se utiliza un modelo pre-entrenado con esos datos),
redimensionado dinámico preservando la relación de aspecto. Solamente se habilita la aumentación
de datos por volteo horizontal, ya que se considera que se dispone de suficiente cantidad de ejemplos adecuados
y medidas de regularización del modelo. Se habilita el agrupado de imágenes por aspecto (se considera que
una imagen es horizontal si es más ancha que larga), para no solo beneficiarse de mayor rendimiento computacional,
sino que también para aumentar la estabilidad de las predicciones del módulo de estimación de parámetros de cámara.
Se filtran ejemplos del conjunto de datos de calibración de cámara SPEC~\cite{SPEC} con valores inválidos para el tipo de imagen (corruptas o vacías), así como ejemplos
considerados no válidos (véase la Figura~\ref{fig:Pano360BadSamples})
utilizando el proyecto de apoyo para visualización y filtrado de imágenes por atributos \code{https://github.com/briansenas/py-img-query}.
Dicho proyecto de apoyo fue desarrollado con fin de permitir filtrar imágenes por atributos calculados como nivel de contraste, luminosidad, número de líneas y
todas aquellas características que facilitaran el filtrado de imágenes redundantes para el entrenamiento del modelo.

\begin{figure}[!ht]
  \centering
  \small
  \subfloat{\includegraphics[width=0.45\textwidth]{imagenes/appendix/23930544131.jpg}}
  \subfloat{\includegraphics[width=0.45\textwidth]{imagenes/appendix/49521868852.jpg}}\\
  \subfloat{\includegraphics[width=0.45\textwidth]{imagenes/appendix/30565712018.jpg}}
  \subfloat{\includegraphics[width=0.45\textwidth]{imagenes/appendix/50462587448.jpg}}
  \caption[Ejemplo de imágenes eliminadas del conjunto de datos durante pre-procesado.]{Se observan ejemplos filtrados del conjunto de calibración de cámara de SPEC~\cite{SPEC}.
  Aunque sean panorámicas, se considera que estos ejemplos no aportan ningún beneficio al entrenamiento del modelo.}
  \label{fig:Pano360BadSamples}
\end{figure}

\subsection{Detalles técnicos de la experimentación}\label{sec:TrainML}
La implementación sobre ``detectron2''~\cite{detectron2} utiliza subclases definidas sobre las clases bases
de los métodos implementados por META para la detección de personas, estimación de pose y segmentación panóptica.
Se modifican ligeramente algunos métodos. Por ejemplo, para permitir el uso de una clase que utiliza una combinación de imágenes
de diferentes conjuntos de datos (3 imágenes de detección de personas y estimación de altura por cada 1 imagen de calibración de
parámetros de la cámara) se modifica el método de inicialización de los datos de entrenamiento.
Además, dado el ratio diferenciado entre imágenes para cada componente del entrenamiento, es necesario modificar la
función que inicializa el optimizador para ponderar dinámicamente cada componente según dicho ratio y el tamaño total
de imágenes por cada iteración (siguiendo la fórmula $\frac{0.1*n_{gpu}*n_{img}}{256}$ de~\cite{goyal2018accuratelargeminibatchsgd}).
Al contrario de los autores, en la primera fase del entrenamiento nuestro modelo parece seguir minimizando los errores pasados las 40K iteraciones, llegando hasta 90K
donde aparenta haber convergido. En la segunda fase parece converger con tan solo 5K iteraciones, con valores de re-proyección muy cercanos a 0.
\par
Para modificar características del entrenamiento se puede modificar el archivo de configuración en \code{configs/COCO-SCALE}.
Dicho archivo permite activar o desactivar recorte del gradiente, calentamiento y recocido del entrenamiento, así como muchas
funcionalidades adicionales. El script \code{train_calib.py} permite replicar los experimentos descritos. Tras lanzar una sesión
de entrenamiento se guardarán resultados utilizando Tensorboard, en la carpeta \code{output}, permitiendo visualizar imágenes
durante el entrenamiento así como las funciones de pérdida y datos auxiliares.
\par
Para la ejecución se utilizaron dos sistemas distintos. En las pruebas de alta demanda de CPU, como extraer los fotogramas y filtrar las
imágenes válidas en ambos conjuntos de datos, se utilizó un ordenador con una CPU AMD Ryzen 7 5800X, 32 GB de RAM DDR4 y una tarjeta
NVIDIA RTX 3060 12GB de VRAM. Para el entrenamiento distribuido en múltiples GPUs, se utilizó el servidor de la
Universidad de Granada con 3 GTX Titan X Pascal, permitiendo disponer de hasta 2 veces más capacidad de cómputo en el mismo tiempo.

\section{Resultados calibración manual}
En la Tabla~\ref{tab:ManualResults} se observan los resultados de
aplicar el método descrito en la Sección~\ref{sec:ManualCalibration}. El error promedio
es de 37 centímetros para todas las cámaras. Destaca la cámara 2 con el mayor error medio, 52 centímetros,
y la mayor varianza, de 30 centímetros. En las Figuras~\ref{fig:ManualWorst}, se observan los ejemplos
con mayor error de medición. El método clásico es susceptible a acumulación de errores por imprecisión en
diferentes pasos del proceso, ya sea por imprecisión de los parámetros estimados de la matriz de proyección
de la cámara, de su posición con relación al sujeto en escena o sus ángulos de rotación. Las imágenes
rectificadas no presentan anomalías evidentes, por lo cual el peso del error recae sobre la calidad
de la estimación del recuadro delimitador (debe ser lo más acertado posible respecto a los pies y cabeza de los individuos),
y la precisión de la posición de la cámara (altura respecto al suelo, distancia al individuo y rotación).
Por otro lado, en las Figuras~\ref{fig:ManualBest} se observan los ejemplos
con una estimación perfecta.
En comparativa, en la Tabla~\ref{tab:BaselineDummy} se muestran
los errores promedios de utilizar un modelo de referencia que predice siempre la altura promedio de 1.75 metros.
El error promedio de dicho método es de 14 centímetros, siendo así 2.6 veces más preciso que el método clásico
para la distribución de los datos actuales.
Los resultados indican que el método clásico depende de mediciones manuales exactas de la escena, así como el uso
de un proceso de calibración de cámara extenso, para lograr errores de estimación aceptables y poder ganar, en promedio, al
método de referencia.
\begin{table}[!ht]
  \centering
  \scriptsize
\begin{tabular}{|l|c|c|c|c|c|c|}
\hline
\rowcolor[HTML]{FFCB2F}
\textbf{Cámara }& \textbf{Soporte }& $\bm{\mu}$ & $\bm{\sigma}$ & $\bm{Q_2}$ & $\bm{Q_3}$ & \textbf{Máximo}\\
\hline
Cam1 & 222 & 0.27 & 0.16 & 0.28 & 0.39 & 0.64 \\
\hline
Cam2 & 293 & 0.52 & 0.30 & 0.48 & 0.74 & 1.43 \\
\hline
Cam3 & 182 & 0.27 & 0.16 & 0.27 & 0.37 & 0.70 \\
\hline
\end{tabular}
\caption[Tabla de errores de estimación del método clásico, expresados en metros.]{
 Tabla de errores de estimación del método clásico, expresados en metros.
 Se observa que, incluso con la cámara calibrada manualmente, el error promedio, de estimación de la altura de los
 individuos en escena, es significativamente alto.
 Destacando que un ejemplo con la cámara 2 obtuvo un error de 1.43 metros.
}
\label{tab:ManualResults}
\end{table}

\begin{figure}[!ht]
  \centering
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/worst_fail_cam1.png}}
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/worst_fail_cam2.png}}\\
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/worst_fail_cam3.png}}
  \caption[Visualización de las peores predicciones del método clásico.]{Se observan ejemplos con las peores mediciones para cada cámara con el método manual, donde el número en verde (superior) es el valor esperado.}\label{fig:ManualWorst}
\end{figure}

\begin{figure}[!ht]
  \centering
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/perfect_cam1.png}}
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/perfect_cam2.png}}\\
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/perfect_cam3.png}}
  \caption[Visualización de predicciones exactas del método clásico.]{Se observan ejemplos de mediciones exactas con cada cámara con el método manual, donde el número verde (superior) es el esperado.}\label{fig:ManualBest}
\end{figure}

\begin{table}[!ht]
  \centering
  \scriptsize
\begin{tabular}{|l|c|c|c|c|c|c|}
\hline
\rowcolor[HTML]{FFCB2F}
\textbf{Cámara }& \textbf{Soporte }& $\bm{\mu}$ & $\bm{\sigma}$ & $\bm{Q_2}$ & $\bm{Q_3}$ & \textbf{Máximo}\\
\hline
Cam1 & 222 & 0.15 & 0.02 & 0.15 & 0.15 & 0.17 \\
\hline
Cam2 & 293 & 0.14 & 0.04 & 0.15 & 0.17 & 0.17 \\
\hline
Cam3 & 182 & 0.13 & 0.05 & 0.17 & 0.17 & 0.17 \\
\hline
\end{tabular}
\caption[Tabla de errores de estimación del estimador estático de referencia, expresados en metros.]{
Tabla de errores de estimación del estimador estático de referencia, expresados en metros.
Se observan los errores promedios en metros del método de referencia que predice siempre la altura promedio de 1.75 metros.
}
\label{tab:BaselineDummy}
\end{table}

\section{Resultados calibración automática}
Para el entrenamiento del modelo se utilizó una tasa de aprendizaje extraída de~\cite{goyal2018accuratelargeminibatchsgd},
con un planificador para reducir su valor conforme se acerca al número total de iteraciones programadas,
con un recorte del gradiente según la norma L2 (para evitar divergencia durante el entrenamiento por conjuntos de ejemplos
ruidosos) y se opta por utilizar el mayor número de imágenes por iteraciones que nos permitan los servidores disponibles y sus capacidades.
El entrenamiento se realiza en dos fases, como descrito en la Sección~\ref{sec:AutoMethod},
donde la primera calibra la estimación de los parámetros de la cámara y el detector de personas por 90K iteraciones.
Finalmente, se realiza un entreno de 5K iteraciones para la estimación de la altura del sujeto y la cámara.
\par
En la Tabla~\ref{tab:AutoResults} se observan los resultados de método de estimación automática. El error promedio de
las cámaras en conjunto es de 14 centímetros, 2.6 veces más pequeño que el método manual.
Un error similar al método de referencia, pero logrando permitir la altura exacta de individuos cuya altura no es 1.75 metros en diversas escenas.
% NOTE: Igual debería obtener una tabla con estos resultados comparativos
% En comparativa, en el artículo de Zhu \emph{et al.} consiguen un error promedio en el conjunto de datos Kitty~\cite{Kitty} y IMDB-25k de 9 centímetros,
% entrenando el modelo hasta 120k iteraciones. Kitty~\cite{Kitty} es un conjunto de datos de conducción autónoma que proporciona
% imágenes y datos de sensores capturados en entornos reales. Por lo tanto, permite comparar la altura estimada frente la real.
% De forma similar, IMDB-25K es una recopilación automática de imágenes e información de altura de personas famosas obtenidas de internet.
% \par
Los valores de mediana y tercer cuartil son mucho menores que el método clásico, dejando ver lo prometedor que puede llegar a ser este método.
Destaca los valores máximos que no llegan a sobrepasar los 50 centímetros de error, mientras que el método de calibración manual ha llegado a tener
un error de 143 centímetros (valor atípico, un error extremo de re-proyección).
En las Figuras~\ref{fig:AutoWorst} y~\ref{fig:AutoBest} se
observan los peores y mejores resultados de este método.
Dado que se conoce la altura de la cámara en las escenas,
se pueden ver reflejados en la Tabla~\ref{tab:AutoCamResults} el error promedio
de estimación del módulo de PointNet~\cite{PointNet} de la altura de la cámara.
Realizar un entrenamiento más largo del modelo puede que reduzca la varianza y el error obtenido,
tanto en la estimación de la altura de la cámara como del sujeto en escena.
En la Tabla~\ref{tab:Results} se recogen los resultados finales
de todos los métodos implementados.
Se observa que el método de aprendizaje profundo evita estimaciones extremas, reduciendo así
los errores máximos observables y, por lo tanto, su error medio.
Obtiene un error promedio similar al estimador de referencia, que siempre predice la altura media
de 1.75 metros, con el beneficio de acertar en múltiples escenas, de diferente sujetos, la estatura exacta.

\begin{table}[!ht]
  \centering
  \scriptsize
\begin{tabular}{|l|c|c|c|c|c|c|}
\hline
\rowcolor[HTML]{FFCB2F}
\textbf{Cámara }& \textbf{Soporte }& $\bm{\mu}$ & $\bm{\sigma}$ & $\bm{Q_2}$ & $\bm{Q_3}$ & \textbf{Máximo}\\
\hline
Cam1 & 222 & 0.21 & 0.11 & 0.19 & 0.26 & 0.43 \\
\hline
Cam2 & 293 & 0.12 & 0.07 & 0.11 & 0.16 & 0.38 \\
\hline
Cam3 & 182 & 0.09 & 0.06 & 0.10 & 0.12 & 0.24 \\
\hline
\end{tabular}
\caption[Tabla de errores de estimación del método por aprendizaje profundo, expresados en metros.]{
  Tabla de errores de estimación del método por aprendizaje profundo, expresados en metros.
  Se observan resultados prometedores del método de estimación automática, semi-supervisado por el error de re-proyección.
Destaca el error medio de la cámara 3, que puede ser debido a su mejor calidad de imagen.}
\label{tab:AutoResults}
\end{table}

\begin{table}[H]
  \centering
  \scriptsize
\begin{tabular}{|l|c|c|c|c|c|c|}
\hline
\rowcolor[HTML]{FFCB2F}
\textbf{Cámara }& \textbf{Soporte }& $\bm{\mu}$ & $\bm{\sigma}$ & $\bm{Q_2}$ & $\bm{Q_3}$ & \textbf{Máximo}\\
\hline
Cam1 & 222 & 0.58 & 0.26 & 0.67 & 0.83 & 0.92 \\
\hline
Cam2 & 293 & 0.38 & 0.13 & 0.35 & 0.44 & 0.76 \\
\hline
Cam3 & 182 & 0.95 & 0.11 & 1.00 & 1.00 & 1.20 \\
\hline
\end{tabular}
\caption[Tabla de errores de estimación de la altura de la cámara con aprendizaje automático, expresados en metros.]{
  Tabla de errores de estimación de la altura de la cámara con aprendizaje automático, expresados en metros.
  Debido a que los datos de entrada a este módulo son estimaciones anteriores, existe una propagación de errores importante que conlleva a una predicción de altura de cámara más ruidosa.
  Por otro lado, existen diversas soluciones de la combinación de las estimaciones (parámetros de cámara, altura del objeto y de la cámara) que pueden llevar a un bajo error de re-proyección.
}
\label{tab:AutoCamResults}
\end{table}

\begin{figure}[!ht]
  \centering
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/worst_fail_cam1_svmiw.png}}
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/worst_fail_cam2_svmiw.png}}\\
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/worst_fail_cam3_svmiw.png}}
  \caption[Visualización de las peores predicciones del método por aprendizaje profundo.]{Se observan ejemplos con las peores mediciones para cada cámara con el método automático, donde el número en verde (superior) es el valor esperado.
  Destaca la restricción sobre las estimaciones que evita errores extremos como los observados en el método manual, ya que el modelo difícilmente estimará valores lejanos al \emph{a priori} estadístico impuesto de 1.75 metros.
Por comparar, en la imagen superior derecha, el método manual estima una altura de 49 centímetros, un error 3.1 veces más grande.}
\label{fig:AutoWorst}
\end{figure}

\begin{figure}[!ht]
  \centering
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/perfect_cam1_svmiw.png}}
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/perfect_cam2_svmiw.png}}\\
  \subfloat{\includegraphics[width=0.5\textwidth]{imagenes/chapter5/perfect_cam3_svmiw.png}}
  \caption[Visualización de predicciones exactas con el método por aprendizaje profundo.]{Se observan ejemplos de mediciones exactas con cada cámara con el método automático, donde el número verde (superior) es el esperado.}
\label{fig:AutoBest}
\end{figure}

Durante el proceso de experimentación se observó que el modelo as veces colapsaba al \emph{a priori},
estimando todos los individuos en la escena la misma altura, cerca del promedio impuesto.
Esto es posible debido a que el modelo puede ajustar tanto la altura de los sujetos en escena,
su profundidad, como la altura de la cámara con respecto al suelo para disminuir
el error de re-proyección. Por ello, un ajuste equilibrado de los pesos de la combinación
lineal de pérdidas es necesario para evitar este colapso. Además, controlar el ratio de
aprendizaje de esta capa, así como la magnitud del gradiente que le afecta, ayuda a
regularizar este comportamiento. No obstante, la solución exacta sería encontrar
una restricción adicional sobre la escena para ayudar a resolver esta ambigüedad.

\begin{table}[!ht]
  \scriptsize
  \begin{center}
    \begin{tabular}{|l|c|c|c|c|c|c|}
      \rowcolor[HTML]{FFCB2F}
      \hline
      \textbf{Cámara } & $\bm{\mu}$ & $\bm{\sigma}$ & $\bm{Q_2}$ & $\bm{Q_3}$ & \textbf{Máximo}\\
      \hline
      Baseline & 0.14 & 0.04 & 0.15 & 0.17 & 0.17 \\
      \hline
      Clásico & 0.37 & 0.26 & 0.33 & 0.49 & 1.42 \\
      \hline
      SVMIW & 0.14 & 0.10 & 0.12 & 0.18 & 0.43 \\
      \hline
    \end{tabular}
  \end{center}
  \caption[Tabla resumen de comparativa final, con errores expresados en metros.]{
  Tabla resumen de comparativa final, con errores expresados en metros.
  Se observa que incluir un \emph{a priori} sobre las estimaciones mejora el error promedio y máximo significativamente.
  El modelo consigue un error mediano inferior al estimador baseline. No obstante, presente una mayor
  variabilidad, con estimaciones con errores hasta 2.5 veces más grandes. Realizar un entrenamiento
  más largo y buscar una ponderación más precisa de la función pérdida podría mejorar dichos resultados.
}\label{tab:Results}
\end{table}
